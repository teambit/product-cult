---
description: A signup page component for the Product Hunt platform.
labels: ['signup', 'form', 'authentication']
---

The `Signup` component allows new users to create an account. It includes input fields for name, email, and password, and handles form submission to create a new user account. If the user is already authenticated, they will be redirected to the homepage.

## Usage

Basic usage of the `Signup` component:

```jsx
<Signup />
```
## Examples
### Basic Signup Form
This example demonstrates the basic signup form with name, email, and password fields.
```jsx
<Signup />
```
### Handling Signup Errors
This example demonstrates how the `Signup` component handles and displays signup errors. When an error occurs during signup, an error message is displayed to the user.
```jsx
<Signup />
```
### Redirecting Authenticated Users
This example demonstrates the `Signup` component's redirection behavior. If a user is already authenticated, they will be automatically redirected to the homepage.
```jsx
<Signup />