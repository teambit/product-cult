/**
 * Defines the options for deleting a product.
 * Corresponds to the DeleteProductOptionsInput in GraphQL schema.
 */
export type DeleteProductOptions = {
  /**
   * The unique identifier of the product to delete.
   */
  id: string;
};